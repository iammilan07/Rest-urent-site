name: Test 2 - Use Variable

on:
  workflow_call:
    inputs:
      shared_variable:
        description: "The variable passed from the main workflow"
        required: true
        type: string

jobs:
  use_input:
    runs-on: ubuntu-latest
    steps:
      - name: Display Shared Variable
        run: |
          echo "The shared variable received in Test 2 is: ${{ inputs.shared_variable }}"